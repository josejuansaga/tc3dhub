version: "3.9"

services:
  tc3d-hub:
    build: ./app
    container_name: tc3d-hub
    ports:
      - "8090:8090"
    env_file:
      - .env
    environment:
      DATA_DIR: /app/data
    volumes:
      - ./data:/app/data
    restart: unless-stopped
